{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "start": "yarn install && npm run build && node .output/server/index.mjs"
  },
  "devDependencies": {
    "@types/three": "^0.140.0",
    "@types/yuka": "^0.7.1",
    "nuxt": "3.0.0-rc.1"
  },
  "dependencies": {
    "cannon-es": "^0.19.0",
    "cannon-es-debugger": "^1.0.0",
    "three": "^0.140.0"
  }
}
